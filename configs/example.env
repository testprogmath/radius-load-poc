# Example environment configuration for the RADIUS load test PoC.
# Copy or export variables as needed. Defaults match the binaries.

# RADIUS server address (auth port)
RADIUS_ADDR=127.0.0.1:1812

# Shared secret between client and server
RADIUS_SECRET=testing123

# Smoke test credentials
RADIUS_USER=testuser
RADIUS_PASS=pass123

# Optional test identifier (sent as Calling-Station-Id)
TEST_ID=

# Per-request timeout
RADIUS_TIMEOUT=2s

# Load generator settings
# Number of synthetic users (user0000..user0999 pattern supported by server)
USERS=1000

# Target steady-state Requests Per Second
RPS=200

# Max in-flight workers (goroutines)
WORKERS=512

# Phase durations
WARMUP=5s
STEADY=30s
SPIKE=10s

# Spike multiplier for RPS
SPIKE_MULT=3.0

# Phase selector: warmup|steady|spike|all
PHASE=all
